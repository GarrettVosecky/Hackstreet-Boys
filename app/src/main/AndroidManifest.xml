<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.hackstreet_boys">

    <!-- Permissions for accessing location and internet -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HackstreetBoys"
        tools:targetApi="31">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCUvFpTBqO3EaMrLjbpy_CDZC01NJJ8GuA" />


        <activity
            android:name=".Sign_inScreen"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity
            android:name=".MapsActivity"
            android:exported="true"
            android:label="@string/app_name">

        </activity>

        <!-- Completed Jobs Page (Explicit Launch & Deep Linking Support) -->
        <activity
            android:name=".CompletedJobsPage"
            android:exported="true">
            <intent-filter>
                <!-- Allows launching via an explicit intent -->
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />

                <!-- Enables deep linking via a web URL -->
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https"
                    android:host="yourapp.com"
                    android:path="/completed"/>
            </intent-filter>
        </activity>

        <!-- Current Jobs Activity (Internal Only) -->
        <activity
            android:name=".current_jobs"
            android:exported="true">
            <!--Temporary Start screen for testing-->
        <!--<intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>-->

        </activity>

        <!-- Sign Up Screen -->
        <activity
            android:name=".Sign_up"
            android:exported="true" />
        <activity
            android:name=".forget_passwordScreen"
            android:exported="true" />





    </application>

</manifest>
